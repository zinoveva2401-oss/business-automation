import type { APIRoute } from 'astro'; import { getVisibleProducts } from '../../lib/content'; import { formatPrice } from '../../lib/site';
export const GET:APIRoute=async()=>{const products=await getVisibleProducts();return new Response(JSON.stringify(products.map(({data})=>({id:data.id,slug:data.slug,title:data.title,description:data.description,type:data.type,category:data.category,status:data.status,price:data.status==='published'&&data.purchaseStatus==='active'?formatPrice(data.price,data.pricePrefix):undefined,cover:data.cover,coverAlt:data.coverAlt}))),{headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'public, max-age=3600'}})};
