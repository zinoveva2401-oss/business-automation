import type { APIRoute } from 'astro'; import { getCatalogProducts } from '../../lib/content'; import { formatPrice } from '../../lib/site';
export const GET:APIRoute=async()=>{const products=await getCatalogProducts();return new Response(JSON.stringify(products.map(({data})=>({id:data.id,slug:data.slug,title:data.title,description:data.description,type:data.type,category:data.category,price:formatPrice(data.price,data.pricePrefix),cover:data.cover,coverAlt:data.coverAlt}))),{headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'public, max-age=3600'}})};
