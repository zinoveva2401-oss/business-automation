---
import { categories } from '../../lib/site';
export function getStaticPaths(){return categories.map(topic=>({params:{slug:topic.slug},props:{topic}}))}
const {topic}=Astro.props;
return Astro.redirect('/articles/?topic='+topic.slug,301);
---
